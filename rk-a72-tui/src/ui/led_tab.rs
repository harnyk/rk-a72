use ratatui::layout::Rect;
use ratatui::Frame;

use crate::app::UiState;
use crate::state::AppState;

pub fn render(_frame: &mut Frame, _area: Rect, _app: &AppState, _ui: &UiState) {
    // LED tab rendering not yet implemented (Task 8)
}
